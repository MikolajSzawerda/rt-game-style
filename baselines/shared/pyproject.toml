[project]
name = "baselines-shared"
version = "0.1.0"
description = "Shared utilities for baseline models"
requires-python = ">=3.10"
dependencies = [
    "tqdm",
]

[tool.uv]
# Standalone project with its own venv for shared scripts

